Required software: `HEASoft`, `XMM SAS`, `BEHR`, `BASH`, `python`.

Initiate with `chmod +x run_pipeline.sh`.
Run program as `./run_pipeline.sh` in BASH.

Required python modules: `astropy`, `astroquery`, `matplotlib`, `numpy`, `pandas`, `requests`, `sas`.
